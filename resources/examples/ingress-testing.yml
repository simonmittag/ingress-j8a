---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-testing
  namespace: testing
spec:
  rules:
    - host: foo.bar.com
      http:
        paths:
          - path: /foo
            pathType: Prefix
            backend:
              service:
                name: s1
                port:
                  number: 80